1. **修改** **`broadcast_user_list`** **方法**：

   * 在 `server.py` 中修改 `broadcast_user_list` 方法，为管理员昵称添加"ADMIN："前缀

   * 检查每个在线用户昵称是否在 `self.admins` 集合中

   * 如果是管理员，将昵称格式化为 `ADMIN：<昵称>`，否则使用原昵称

2. **添加** **`broadcast_user_list()`** **调用**：

   * 在服务器控制台的 `op` 命令处理中添加 `broadcast_user_list()` 调用

   * 在服务器控制台的 `unop` 命令处理中添加 `broadcast_user_list()` 调用

   * 确保当管理员权限变更时，所有客户端的用户列表都会更新

3. **修改后的代码流程**：

   * 当用户被设为管理员时，调用 `broadcast_user_list()` 更新所有客户端的用户列表

   * 当管理员权限被撤销时，同样调用 `broadcast_user_list()` 更新用户列表

   * 客户端接收到用户列表后，直接显示格式化后的昵称，无需额外处理

4. **测试验证**：

   * 设置管理员权限，验证用户列表中显示为 `ADMIN：<昵称>`

   * 撤销管理员权限，验证用户列表中恢复为原昵称

   * 确保所有客户端的用户列表同步更新

